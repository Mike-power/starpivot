# 地面侧服务（ground/）

地面侧：32B 大模型经 vLLM 部署，负责复杂规划与兜底（前缀缓存 + 工具调用投机执行，12 月实现）。

## 当前内容

- `rag_demo/` —— 阶段 0 周末任务：最小 RAG demo（加载 → 切分 → FAISS 向量检索 → 千问生成），也是 11 月「星枢」Agent 骨架的知识问答基础。先跑通它，周日验收。

## 路线图

| 时间 | 内容 |
|------|------|
| 2026.9 底 | 最小 RAG demo 跑通（当前任务） |
| 2026.11 | 升级为 Agent 工作流骨架（LangGraph 编排，测延迟基线） |
| 2026.12 | vLLM 部署 32B + 前缀缓存跨步复用 + KV cache 量化实测 |
